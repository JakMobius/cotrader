# @package _global_
model:
  _target_: cotrader.models.baseline_zero.BaselineZeroModel
  sequence_length: 1

constants:
  model_codename: baseline_zero

data:
  normalize_input_mean: false
  normalize_output_mean: false

training:
  gradient_clip_val: null
  epochs: 1
